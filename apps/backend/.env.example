PORT=4000
NODE_ENV=development
JWT_SECRET=dev-only-secret-key-change-in-production

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/survival_spanish
DB_POOL_MIN=2
DB_POOL_MAX=10

# LLM Providers (optional; falls back to mock if not set)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
